{
  "kind": "build_request",
  "title": "Automatically grant administrator role to users with username 'admin'",
  "projectName": "Sanchaari",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-72",
      "text": "Modify the user registration function in backend/main.mo to automatically check if the username is 'admin' (case-insensitive) and assign the 'administrator' role to that user account, overriding any role selection from the registration form",
      "target": "backend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "Admin username is one whose account username is admin that's the admin master account"
        ]
      },
      "acceptanceCriteria": [
        "When a user registers with username 'admin', they are automatically assigned the 'administrator' role regardless of the role they selected during signup",
        "The username check is case-insensitive (e.g., 'Admin', 'ADMIN', 'admin' all trigger admin role assignment)",
        "The admin user has immediate access to the admin dashboard at /admin route after registration",
        "Other usernames are unaffected and receive their selected role as normal"
      ]
    }
  ],
  "constraints": [
    "Do not change the backend/main.mo single-actor architecture",
    "Do not modify the existing UserProfile data model fields",
    "Do not change the frontend registration form UI or role selection dropdown"
  ],
  "nonGoals": [
    "Creating separate admin login credentials or authentication system",
    "Adding hardcoded principal IDs for admin access",
    "Modifying the admin dashboard access control logic",
    "Changing how other roles are assigned during registration"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}